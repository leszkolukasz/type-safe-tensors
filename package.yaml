name: hasktorch
language: GHC2021

default-extensions:
  - TypeFamilies
  - DataKinds
  - GADTs
  - AllowAmbiguousTypes
  - UndecidableInstances

library:
  source-dirs: lib
  dependencies:
    - base
    - vector

executables:
  example:
    main: Main.hs
    source-dirs: app
    dependencies:
      - base
      - hasktorch
      - vector

tests:
  hasktorch-test:
    main: Spec.hs
    source-dirs: test
    dependencies:
      - base
      - hasktorch
      - hspec
      - vector
